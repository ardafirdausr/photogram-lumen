<?php

use Illuminate\Database\Seeder;

class DummyData extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // Use Faker and query builder
        // $faker = Faker::create();
        // for($i = 0; $i < 10; $i++){
            // DB::table('Users')->insert([
            //     'name' => $faker->name,
            //     'email' => $faker->email,
            //     'password' => Crypt::encrypt($faker->password)
            // ]);            
        // }
    }
}
